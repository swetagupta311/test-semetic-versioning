version: '3.8'

services:
  pmi-uat-testing:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - ./.env
    volumes:
    pmi: pmi

  Mysql:
    image: "mysql:latest"
    ports: "3306:3306"
    
